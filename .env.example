# Backend Supabase credentials (service role key for server-side operations)
# WARNING: Never commit the actual .env file with real values to git
SUPABASE_URL=your_supabase_url_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Frontend Supabase credentials (anon key for client-side auth)
# These are exposed to the client, so ensure RLS is properly configured
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Cakto (frontend)
VITE_CAKTO_CHECKOUT_URL=https://pay.cakto.com.br/your-checkout

# Admin bypass (optional)
# Comma-separated list of emails that can access protected routes even if subscription is blocked.
# VITE_ADMIN_EMAILS=admin1@example.com,admin2@example.com

# Cakto (Edge Function secrets)
# CAKTO_WEBHOOK_SECRET=your_webhook_fields_secret
# CAKTO_CLIENT_ID=your_client_id
# CAKTO_CLIENT_SECRET=your_client_secret

# Logging (optional, defaults: development=debug, production=info)
# LOG_LEVEL=debug

# Production domain for CORS (optional, add to server/index.ts allowedOrigins when deploying)
# PRODUCTION_DOMAIN=https://yourdomain.com
